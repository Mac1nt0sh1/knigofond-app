# ===========================================
# БАЗА ДАННЫХ (Supabase PostgreSQL)
# ===========================================
# Получите эти значения в Supabase Dashboard:
# Project Settings → Database → Connection string

# Transaction pooler connection (для Prisma Client)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (для миграций)
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# ===========================================
# NEXTAUTH
# ===========================================
# URL вашего приложения
# Для локальной разработки:
NEXTAUTH_URL="http://localhost:3000"

# Для продакшена на Vercel:
# NEXTAUTH_URL="https://your-app-name.vercel.app"
# ИЛИ оставьте пустым - Vercel автоматически установит через VERCEL_URL

# Секретный ключ (сгенерируйте: openssl rand -base64 32)
# В Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# В Linux/Mac: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-min-32-characters"

# ===========================================
# PRODUCTION (Vercel)
# ===========================================
# На Vercel установите эти переменные в:
# Project Settings → Environment Variables → Add New
#
# 1. NEXTAUTH_URL (опционально):
#    - Оставьте пустым, и Vercel автоматически использует VERCEL_URL
#    - ИЛИ установите вручную: https://your-app-name.vercel.app
#
# 2. NEXTAUTH_SECRET (обязательно):
#    - Сгенерируйте ключ: openssl rand -base64 32
#    - Или используйте онлайн генератор
#    - Минимум 32 символа
#
# 3. DATABASE_URL - из Supabase (pooler connection)
# 4. DIRECT_URL - из Supabase (direct connection)
#
# После добавления переменных пересоберите проект (Redeploy)
